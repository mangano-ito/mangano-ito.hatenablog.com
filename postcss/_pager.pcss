.pager {
    display: flex;
    margin: 5em 0;
    flex-wrap: wrap;

    & > * {
        flex-basis: 20em;
        margin: 1em 0;
    }

    & .pager-next {
        flex: 1;
        text-align: right;
    }
}
